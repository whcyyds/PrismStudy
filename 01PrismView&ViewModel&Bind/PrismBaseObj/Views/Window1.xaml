<Window x:Class="PrismBaseObj.Views.Window1"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:PrismBaseObj.Views"
        mc:Ignorable="d"
        Title="Window1" Height="450" Width="800">
    <StackPanel Orientation="Vertical" >
        <TextBox Height="30" Width="120"  Text="{Binding UserInfo.IdCard}"/>
        <TextBox Height="30" Width="120"  Text="{Binding UserInfo.UserName}"/>
        <Button Height="30" Width="120" Margin="0,5,0,0" Command="{Binding EditCmm}" CommandParameter="{Binding UserInfo}">修改名称</Button>
    </StackPanel>
</Window>
